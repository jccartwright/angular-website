'use strict';

describe('myApp.wms_services module', function() {

  beforeEach(module('myApp.wms_services'));

  describe('wms_services controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var wmsServicesCtrl = $controller('WmsServicesCtrl');
      expect(wmsServicesCtrl).toBeDefined();
    }));

  });
});